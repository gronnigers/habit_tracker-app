/* ============================================================
   Habit Tracker — configuration
   Fill these in from your Entra app registration, then commit.
   ============================================================ */
window.HABIT_CONFIG = {
  // Entra > App registrations > your app > Overview
  clientId: "PASTE_YOUR_APPLICATION_CLIENT_ID",
  tenantId: "PASTE_YOUR_DIRECTORY_TENANT_ID",

  // Optional. Leave null to auto-use the page's own URL.
  // If you set it, it MUST exactly match a Redirect URI registered
  // under Authentication > Single-page application.
  redirectUri: null
};
